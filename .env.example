# AWS Configuration
AWS_REGION=ap-northeast-1
AWS_ACCOUNT_ID=your-account-id

# S3 Bucket（単一バケット構造）
S3_BUCKET_NAME=avc-system-{your-account-id}

# DynamoDB
DYNAMODB_TABLE_NAME=Documents

# SES
SES_SENDER_EMAIL=noreply@example.com
SES_RECEIVE_DOMAIN=mail.example.com

# API（デプロイ後に infra からの出力値を設定）
API_ENDPOINT=https://24bdzigj8k.execute-api.ap-northeast-1.amazonaws.com

# Frontend specific
VITE_API_URL=https://24bdzigj8k.execute-api.ap-northeast-1.amazonaws.com

# Backend Lambda Environment Variables（CDKで自動設定されるため手動設定不要）
# BUCKET_NAME=fax-system-{account-id}
# TABLE_NAME=Documents
# REGION=ap-northeast-1
